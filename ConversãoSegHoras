#include <stdio.h>

int main(void) {
    int s;
    int hh;
    int mm;
    int ss;

    scanf("%d", &s);

    hh = s / 3600;
    mm = (s % 3600) / 60;
    ss = (s % 3600) % 60;

    printf("%02d:%02d:%02d", hh, mm, ss);

    return 0;
}
