#include <stdio.h>

int main() {
    int n1;
    int n2;
    int n3;
    int menor;

    scanf("%d %d %d", &n1, &n2, &n3);

    menor = n1;

    if (menor > n2) {
        menor = n2;
    }
    if (menor > n3) {
        menor = n3;
    }

    printf("%d", menor);

    return 0;
}
