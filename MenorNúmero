#include <stdio.h>

int main() {
    int n1;
    int n2;

    scanf("%d %d", &n1, &n2);

    if (n1 > n2) {
        printf("%d", n1);
    } else if (n2 > n1) {
        printf("%d", n2);
    } else {
        printf("%d", n1);
    }
    return 0;
}
